services:
  - type: web
    name: Titanos-leadgen
    env: node
    buildCommand: npm ci && npm run build && npx puppeteer browsers install chrome --path ./dist/browser_bin
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
